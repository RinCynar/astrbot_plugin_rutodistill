{
  "distill_model": {
    "type": "list",
    "description": "蒸馏模型指定",
    "hint": "从当前 Provider 启用的模型中选择（AstrBot 启动后自动填充选项）；留空则跟随 Provider 默认模型。也可在聊天中使用 /r-info 指令查看与设置。",
    "default": [],
    "options": []
  },
  "opening_word_use_prob": {
    "type": "int",
    "description": "开场词使用概率",
    "hint": "掌握开场词后，插件在每条回复发起前自行判定：约该比例（%）的回复允许以一个开场词开头（从已掌握的开场词中随机轮换），其余回复硬性禁止开场词、直接切入话题。0 = 禁用开场词，100 = 每次必用。",
    "default": 50,
    "min": 0,
    "max": 100
  },
  "detail_merge_interval": {
    "type": "string",
    "description": "细节库定期整理周期",
    "hint": "定期检查细节库中可合并的重复/近义条目并做无损合并；选择“从不”则仅保留写入时的精确去重。整理在每次蒸馏后按周期触发。",
    "default": "每周",
    "options": ["每天", "每三天", "每周", "每半个月", "每个月", "从不"]
  }
}

