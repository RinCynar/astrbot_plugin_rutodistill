{
  "distill_scope": {
    "type": "string",
    "description": "蒸馏作用域与数据隔离",
    "hint": "按用户隔离：每个发送者拥有独立专属人格（推荐，避免群聊中多人风格混淆）；按会话隔离：以当前群/私聊会话为单位共享同一份人格。",
    "default": "按用户隔离（推荐）",
    "options": ["按用户隔离（推荐）", "按会话隔离"]
  },
  "distill_model": {
    "type": "list",
    "description": "蒸馏模型指定",
    "hint": "从当前 Provider 启用的模型中选择（AstrBot 启动后自动填充选项）；留空则跟随 Provider 默认模型。也可在聊天中使用 /r-info 指令查看与设置。",
    "default": [],
    "options": []
  },
  "detail_merge_interval": {
    "type": "string",
    "description": "细节库定期整理周期",
    "hint": "定期检查细节库中可合并的重复/近义条目并做无损合并；选择“从不”则仅保留写入时的精确去重。整理在每次蒸馏后按周期触发。",
    "default": "每周",
    "options": ["每天", "每三天", "每周", "每半个月", "每个月", "从不"]
  }
}
