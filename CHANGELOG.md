# 更新日志

## 1.0.8 (2026-08-08)

### 修复
- **AstrBot 无法加载插件（配置类型 `str` 不受支持）**：`detail_merge_interval` 配置项在 `_conf_schema.json` 中误用 `"type": "str"`，而 AstrBot 仅支持 `int/float/bool/string/text/list/file/object/template_list/dict`，导致加载时抛 `TypeError` 插件整体加载失败；已改为 `"type": "string"`。
- **标点语癖未被遵守**：注入侧新增「标点红线（最高优先级）」——当已蒸馏特征表明目标用户不使用句末标点/逗号（以空格或换行断句）时，明确要求克隆体回复正文同样禁用这些标点、以空格或换行表达停顿与反问，并增加发送前自检要求；同时规定仅允许使用用户确实使用的符号（全角引号、全角破折号、括号、ASCII 省略号等）。用户使用标点的场景则自动回退为「跟随对方标点习惯」的通用约束。
- **开场词学了就必须用 / 机械复用**：开场词策略改为「概率制」——新增插件设置 `opening_word_use_prob`（0-100，默认 50）。掌握开场词后，回复按该比例从已掌握的开场词中**随机抽取一个**使用，其余比例**直接切入话题不使用开场词**；即使只掌握一个开场词也不应每条都用。0 = 禁用开场词，100 = 每次必用（仍随机抽取、避免每次都抽中同一个）。
- **信息量不足 / 长期记忆受限**：新增**细节库（details）** 机制——逐条累积具体可观测的离散细节（口头禅、句式、标点/空格习惯、括号表情、话题偏好、具体立场等），**不设存储上限**，可支撑上千轮蒸馏的长期记忆沉淀，不再受「单段概括」结构的饱和限制。
  - 提取侧：`new_details` 增量字段 + 「summary 概述层保持精简、细节进细节库」分工，概述字段不再无限膨胀；
  - 注入侧：细节库按 1200 字符预算随人格 prompt 注入，近期细节优先；
  - 导出侧：`/r-export` 人设模板新增完整「细节库」章节；`/r-status` 显示细节条数；
  - 提取请求锚点展示最近 40 条细节用于去重，避免重复收录。
- **细节库定期整理**：新增插件设置 `detail_merge_interval`（每天/每三天/每周/每半个月/每个月/从不，默认每周）。每轮蒸馏后按周期检查，对重复/近义细节执行**无损合并**——保持原有顺序、除重复/近义条目外逐字保留、不概括不丢信息；写入时的精确去重始终生效。整理请求超长时只整理最近部分并原位替换，保证旧条目不丢失。
- **存储与展示容量提升**：金句示例上限 50 → 200，注入展示 5 → 8，提取锚点 10 → 20；历史消息单条截断长度 600 → 1000 字符。

### 变更
- `PersonaProfile.details` 新增字段（旧存储数据无该字段时自动回退为空，兼容）；`SessionMetrics.details_last_merge_ts` 记录细节库最近一次整理时间。
- `PromptBuilder.build_prompts` 新增 `opening_word_use_prob` 参数；`DistillerEngine` 新增 `parse_consolidate_json` 与 `CONSOLIDATE_SYSTEM_PROMPT`（细节库无损合并专用）；移除 `max_details` 参数（细节库不设上限）。

---

## 1.0.7 (2026-08-08)

### 修复
- **标点语癖未被遵守**：注入侧新增「标点红线（最高优先级）」——当已蒸馏特征表明目标用户不使用句末标点/逗号（以空格或换行断句）时，明确要求克隆体回复正文同样禁用这些标点、以空格或换行表达停顿与反问，并增加发送前自检要求；同时规定仅允许使用用户确实使用的符号（全角引号、全角破折号、括号、ASCII 省略号等）。用户使用标点的场景则自动回退为「跟随对方标点习惯」的通用约束。
- **开场词机械复用**：新增「不固定开口词/开场白」规则，避免克隆体每条回复都以同一个词（如“呐…”）开头；标志性口头禅上限清单补充“呐…”等词。
- **信息量不足 / 长期记忆受限**：新增**细节库（details）** 机制——逐条累积具体可观测的离散细节（口头禅、句式、标点/空格习惯、括号表情、话题偏好、具体立场等），存储上限放宽至 1000 条，可支撑上百上千轮蒸馏的细节沉淀，不再受「单段概括」结构的饱和限制。
  - 提取侧：`new_details` 增量字段 + 「summary 概述层保持精简、细节进细节库」分工，概述字段不再无限膨胀；
  - 注入侧：细节库按 1200 字符预算随人格 prompt 注入，近期细节优先；
  - 导出侧：`/r-export` 人设模板新增完整「细节库」章节；`/r-status` 显示细节条数；
  - 提取请求锚点展示最近 40 条细节用于去重，避免重复收录。
- **存储与展示容量提升**：金句示例上限 50 → 200，注入展示 5 → 8，提取锚点 10 → 20；历史消息单条截断长度 600 → 1000 字符。

### 变更
- `PersonaProfile.details` 新增字段（旧存储数据无该字段时自动回退为空，兼容）；`DistillerEngine` 新增 `max_details` 参数（默认 1000）。

---

## 1.0.6 (2026-08-08)

### 修复
- **拟态修辞过拟合**：注入侧新增标志性口头禅/连接词使用上限——若目标用户有固定标志词（如『换言之』『简言之』『我倒是觉得吧』），每条回复至多使用一次，严禁连续以同类标志词开启多个段落，避免学舌式仿写。
- **领域切换失灵**：注入侧新增领域切换约束——话题涉及技术、硬件、配置、数值、规则等硬内容时，切换到简洁直给（可列点）的表达，停止抒情散文腔；娱乐、文学、情感类话题再回到原文抒情节奏。
- **固定提问收尾**：基础闲聊约束与拟态约束均新增「不以提问收尾」规则，仅当真正想了解答案时才反问。
- **篇幅密度倒挂**：新增篇幅控制约束，回复长度接近或短于对方消息，禁止用空泛景语、排比、抒情填充长度，保持目标用户高密度表达节奏。
- **语气语义错读**：新增 `tone`（语气/情绪色彩）蒸馏维度，覆盖戏谑、吐槽、认真论证、平静陈述、敷衍、兴奋等情绪表达方式；注入侧新增「语气同频」约束，避免把戏谑误读为烦躁（如 awa（） 被读成"抓狂"的问题）。
- **格式保真残余漏网**：提取 Prompt 强化标点逐字符保真——ASCII 连续句点（"......"/"..."）必须如实描述，严禁改写为 "…" 等全角/Unicode 标点。

### 变更
- `PersonaProfile` / `ProfilePatch` / `/r-status` 状态卡片 / 蒸馏提取请求锚点 / `/r-export` 人设模板均新增 `tone` 字段；旧存储数据无该字段时自动回退为空（兼容）。

---

## 1.0.5 (2026-08-08)

### 修复
- **蒸馏特征被单轮输入覆盖丢失**（核心问题）：提取 Prompt 新增「累积而非覆盖」硬约束——必须完整保留仍然成立的既有特征，并与新特征整合成更全面的表述；风格差异大时（如文学评论切换到硬件清单）并列保留两者，严禁只按最新输入重写字段。
- **蒸馏上下文不足**：SessionState 新增 `history` 字段累积最近 10 轮用户消息逐字原文，蒸馏请求现在携带「近期用户表达上下文」+「既有特征锚点」+「最新输入」，使人格随轮次逐步立体丰满。
- **并发覆盖**：每个会话增加独立 `asyncio.Lock`，串行化后台蒸馏任务，防止并发消息互相覆盖已累积历史。
- 金句示例上限 5 → 50 条（长期保留不再过早挤出），注入侧典型示例展示 3 → 5 条。

### 优化
- 新增 `_build_extraction_prompt()` 统一构造提取请求，历史单条超长消息截断（600 字符）以控制上下文长度。
- 提取请求中的金句锚点仅展示最近 10 条（`EXAMPLES_ANCHOR_MAX`），避免金句库扩大后撑爆上下文。

---

## 1.0.4 (2026-08-08)

### 修复
- **蒸馏标点幻觉**：提取 Prompt 新增「格式保真」强约束——只允许描述原文真实存在的标点/空格/格式，严禁把 LLM 自动补全的标点误认为用户手笔；金句示例必须逐字引用原文，不得增删改任何字符。
- **个性化保留不足**：特征描述要求保留具体可还原的细节（语气词、口头禅、句式、引用习惯等），避免泛化概括；蒸馏请求中补充金句示例上下文。
- **`/r-export` 全局前提第四条**措辞简化为「自然地直率抒发情感」。
- **版本号统一**：`main.py` @register 版本与 `metadata.yaml` 对齐。

### 优化
- **`/r-start` 引导话题由 LLM 自主生成**：不再使用呆板的固定模板列表，改为调用 LLM 生成接近真人朋友口吻的开放式问题，并可结合已蒸馏偏好；生成失败时自动回退到内置模板。
- 新增 `_get_provider()` 统一 Provider 获取逻辑（兼容同步/异步 API），消除重复代码。

---

## 1.0.3

### 修复
- 移除提取结果与注入 Prompt 中的括号表情诱导；拟态特征改为**前置注入**（位于 AstrBot 默认 system prompt 之前），提升模型遵循度。
- 新增「写作格式要求」：标点/空格/括号表情等格式习惯作为强制模仿维度。
- 收敛度基于最近 N 轮特征客观变化幅度计算，与全局配置中的死参数解耦。

---

## 1.0.2

### 变更
- 指令重构为 `r-` 前缀结构：`/r-start`（蒸馏/初始化）、`/r-lock`（静态锚定-拟态）、`/r-status`、`/r-info`、`/r-export`。
- 插件展示名改为 **世另我**。
- 拟态特征注入顺序调整：放在 AstrBot 默认 prompt 之前。
- 去除通用约束中的括号表情诱导，仅在目标用户确有此类习惯时使用。

